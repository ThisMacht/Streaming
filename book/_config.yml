title: Big Data Lab 04 - Spark Streaming
author: Team Vessel

execute:
  # Preserve the tracked, already-executed evidence outputs. Re-execution would
  # depend on runtime artifacts and is outside the documentation build.
  execute_notebooks: "off"

sphinx:
  config:
    exclude_patterns:
      - README.md

html:
  use_repository_button: true
  use_issues_button: false
  use_edit_page_button: false

repository:
  url: https://github.com/ThisMacht/Streaming
  branch: main
